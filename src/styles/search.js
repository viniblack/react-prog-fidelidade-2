import styled from 'styled-components';

const ButtonSearch = styled.button`
  background-color: var(--color-title-nav);
  border: none;
  font-size: 1rem;
  padding: .2rem;

  border-radius: 0 7px 7px 0 ;
  box-shadow: 4px 4px var(--color-box-shadow);
`;

export { ButtonSearch as default };
