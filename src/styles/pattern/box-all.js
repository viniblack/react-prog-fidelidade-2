import styled from 'styled-components';

const BoxAll = styled.div((props) => ({
  /* margin: 10px; */
  /* box-shadow: 19px 15px 50px 10px #a5a5a5; */
  borderRadius: '19px',
  marginBottom: props.margin,
}));

export { BoxAll as default };
